{% extends 'base.html.twig' %}

{% block title %}Search{% if query %} - {{ query }}{% endif %} - Atlas Bookshop{% endblock %}

{% block body %}
<div class="container my-4">
    <h1 class="brand-serif"><i class="bi bi-search"></i> Search the shelves</h1>
    
    <form method="get" action="{{ path('app_search') }}" class="mb-4">
        <div class="input-group">
            <input type="text" name="q" class="form-control form-control-lg" placeholder="Search for books, authors, or genres..." value="{{ query }}">
            <button type="submit" class="btn btn-primary btn-lg">
                <i class="bi bi-search"></i> Search
            </button>
        </div>
    </form>

    {% if query %}
        {% if products is empty %}
            <div class="alert alert-info text-center py-5">
                <i class="bi bi-search display-1"></i>
                <h4 class="mt-3">No titles found</h4>
                <p>Try searching by author, title, or a memorable keyword.</p>
            </div>
        {% else %}
            <p class="text-muted mb-3">Found {{ products|length }} title(s) for "{{ query }}"</p>
            <div class="row g-4">
                {% for product in products %}
                    <div class="col-md-6 col-lg-3">
                        {% include 'components/product-card.html.twig' with {'product': product} %}
                    </div>
                {% endfor %}
            </div>
        {% endif %}
    {% else %}
        <div class="alert alert-secondary text-center py-5">
            <i class="bi bi-info-circle display-1"></i>
            <h4 class="mt-3">Enter a search term</h4>
            <p>Search for books by title, author, or keywords.</p>
        </div>
    {% endif %}
</div>
{% endblock %}

